section.education
  +subtitle("education")

  .page__qualifications
    ul.qualifications
      - for (var i = 0; i < 4; i += 1)
        +qualification({
          degree: "Master degree",
          place: "University name"
        })